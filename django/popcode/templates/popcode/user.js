document.getElementById('settingsForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // should implement saving (user info) logic
    alert('Settings saved!');
});

function handleLogout() {
    // should implement logout logic
    alert('Logged out!');
}